# 🔍 مشروع مراقبة متجر المصباح للإضاءة

<div dir="rtl">

## 📋 نظرة عامة

بوت ذكي لمراقبة التغييرات في منتجات قسم **ثريات كريستال أوروبي** بمتجر المصباح للإضاءة، مع إرسال تنبيهات فورية عبر التيليجرام.

### ✨ المميزات

- 🆕 **اكتشاف المنتجات الجديدة** - تنبيه فوري عند إضافة منتج جديد
- ⚠️ **تتبع نفاد الكمية** - إشعار عند نفاد المنتجات
- ✅ **عودة التوفر** - تنبيه عند عودة توفر المنتجات
- 🗑️ **حذف المنتجات** - اكتشاف المنتجات المحذوفة
- 💰 **تغييرات الأسعار** - مراقبة تغييرات الأسعار
- 📊 **تقارير دورية** - ملخص شامل كل ساعة
- 💾 **نسخ احتياطي** - حفظ تلقائي لقاعدة البيانات
- 🤖 **تشغيل تلقائي** - عبر GitHub Actions (مجاناً)

---

## 🏗️ هيكلية المشروع

```
Almusbah_Monitor/
├── .github/
│   └── workflows/
│       └── monitor.yml          # أتمتة GitHub Actions
├── backups/                     # النسخ الاحتياطية
├── config.py                    # الإعدادات
├── database.py                  # إدارة قاعدة البيانات
├── scraper.py                   # سحب البيانات
├── notifier.py                  # الإشعارات
├── main.py                      # البرنامج الرئيسي
├── requirements.txt             # المكتبات
├── products.db                  # قاعدة البيانات
├── monitor.log                  # السجلات
└── README.md                    # هذا الملف
```

---

## 🚀 خطوات التشغيل

### 1️⃣ إنشاء بوت تيليجرام

1. افتح [@BotFather](https://t.me/BotFather) في التيليجرام
2. أرسل `/newbot`
3. اختر اسم ومعرف للبوت
4. احفظ الـ **Token** (مثل: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 2️⃣ الحصول على Chat ID

1. أرسل رسالة لبوتك
2. افتح: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. ابحث عن `"chat":{"id":123456789`
4. احفظ الرقم (Chat ID)

### 3️⃣ رفع المشروع على GitHub

```bash
# إنشاء مستودع جديد
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/almusbah-monitor.git
git push -u origin main
```

### 4️⃣ إضافة Secrets في GitHub

1. اذهب لـ `Settings` → `Secrets and variables` → `Actions`
2. اضغط `New repository secret`
3. أضف:
   - **Name:** `TELEGRAM_TOKEN` → **Value:** التوكن من BotFather
   - **Name:** `TELEGRAM_CHAT_ID` → **Value:** الـ Chat ID

### 5️⃣ تفعيل GitHub Actions

1. اذهب لتبويب `Actions`
2. اضغط `I understand my workflows, go ahead and enable them`
3. سيعمل البوت تلقائياً كل ساعة!

---

## 🧪 التشغيل المحلي (اختياري)

### المتطلبات
- Python 3.9+
- حساب Telegram

### التثبيت

```bash
# تثبيت المكتبات
pip install -r requirements.txt

# تعيين متغيرات البيئة
export TELEGRAM_TOKEN="your_token_here"
export TELEGRAM_CHAT_ID="your_chat_id_here"

# تشغيل البرنامج
python main.py
```

---

## 📊 كيفية عمل المشروع

### 1. جمع البيانات
- يزور البوت صفحة القسم المحدد
- يسحب معلومات جميع المنتجات (الاسم، السعر، الحالة، الرابط)
- يدعم التنقل بين الصفحات تلقائياً

### 2. المقارنة والتحليل
```python
# مقارنة الحالة الحالية مع قاعدة البيانات
old_products = db.get_all_products()      # من قاعدة البيانات
current_products = scraper.get_products()  # من الموقع

# اكتشاف التغييرات
- منتجات جديدة: موجودة في الموقع، غير موجودة في القاعدة
- نفاد كمية: تغيرت الحالة من Available → Out of Stock
- عودة توفر: تغيرت الحالة من Out of Stock → Available
- حذف منتجات: موجودة في القاعدة، غير موجودة في الموقع
- تغيير أسعار: السعر مختلف
```

### 3. الإشعارات
- يرسل رسالة منفصلة لكل تغيير
- يرسل تقرير ملخص في النهاية

### 4. الحفظ
- يحدّث قاعدة البيانات
- يحفظ نسخة احتياطية
- GitHub Actions يحفظ التغييرات تلقائياً

---

## 📱 أمثلة على الإشعارات

### منتج جديد
```
🆕 منتج جديد تم إضافته!
━━━━━━━━━━━━━━━━━━━━
📦 الاسم: نجف مودرن ذهبي قطر 80 سم
💰 السعر: 460.00
🔗 الرابط: [عرض المنتج]
━━━━━━━━━━━━━━━━━━━━
⏰ تم الاكتشاف: 2024-01-15 14:30:00
```

### نفاد كمية
```
⚠️ تنبيه نفاد كمية!
━━━━━━━━━━━━━━━━━━━━
📦 الاسم: نجف شيمر لق كريستال
💰 السعر: 460.00
❌ الحالة: المنتج أصبح غير متوفر
🔗 الرابط: [عرض المنتج]
```

### تقرير الملخص
```
📊 تقرير الفحص الدوري
━━━━━━━━━━━━━━━━━━━━
📅 التاريخ: 2024-01-15 15:00:00
🔍 القسم: ثريات كريستال أوروبي

📈 الإحصائيات:
• إجمالي المنتجات: 330
• المتوفرة: 285
• غير المتوفرة: 45

🔄 التغييرات المكتشفة:
• منتجات جديدة: 2
• نفاد كمية: 3
• عودة توفر: 1
• منتجات محذوفة: 0
• تغييرات أسعار: 1
```

---

## 🎬 السيناريو التجريبي (للعرض)

لإنشاء **Demo** احترافي يعرض جميع الحالات:

### 1. تشغيل أولي
```bash
python main.py
```
- سيسحب جميع المنتجات ويحفظها

### 2. التلاعب بالبيانات (لتوليد التغييرات)

استخدم [DB Browser for SQLite](https://sqlitebrowser.org/) لفتح `products.db`:

#### لاختبار "نفاد كمية":
```sql
-- اختر منتج متوفر وغيره لنافد
UPDATE products
SET status = 'Available'
WHERE id = 'نجف-شيمر-لق-كريستال'
  AND status = 'Out of Stock';
```

#### لاختبار "منتج جديد":
```sql
-- احذف منتج موجود
DELETE FROM products WHERE id = 'نجف-مودرن-ذهبي';
```

#### لاختبار "حذف منتج":
```sql
-- أضف منتج وهمي
INSERT INTO products VALUES
('test-123', 'نجف تجريبي للحذف', 'https://example.com', '999.00', 'Available',
 datetime('now'), datetime('now'), 1);
```

#### لاختبار "تغيير سعر":
```sql
-- غيّر السعر
UPDATE products
SET price = '299.00'
WHERE id = 'نجف-كريستال-فاخر';
```

### 3. التشغيل الثاني (للتسجيل)
```bash
# سجل الشاشة
python main.py
```

سترى جميع أنواع التغييرات والإشعارات! 🎥

---

## ⚙️ التخصيص

### تغيير القسم المراقب
في `config.py`:
```python
CATEGORY_URL = "رابط_القسم_الجديد"
```

### تغيير توقيت الفحص
في `.github/workflows/monitor.yml`:
```yaml
schedule:
  - cron: '0 */2 * * *'  # كل ساعتين
  - cron: '0 9,21 * * *' # الساعة 9 صباحاً و9 مساءً فقط
```

### تخصيص الرسائل
في `config.py` → `MessageTemplates`

---

## 🔧 استكشاف الأخطاء

### البوت لا يعمل؟
1. ✅ تأكد من Secrets في GitHub
2. ✅ تحقق من `Actions` → آخر تشغيل → Logs
3. ✅ تأكد من صلاحيات البوت (أرسل له `/start`)

### لا توصل إشعارات؟
```bash
# اختبار الاتصال محلياً
python -c "from notifier import TelegramNotifier; TelegramNotifier().test_connection()"
```

### قاعدة البيانات لا تُحفظ؟
- تأكد من وجود `GITHUB_TOKEN` (تلقائي)
- تحقق من الـ Push permissions في Settings

---

## 📈 الإحصائيات المتقدمة

المشروع يحفظ:
- سجل كامل بكل التغييرات
- إحصائيات دورية
- عدد مرات الفحص لكل منتج

استعلام مثال:
```sql
-- أكثر المنتجات تغييراً
SELECT product_id, COUNT(*) as changes
FROM change_history
GROUP BY product_id
ORDER BY changes DESC
LIMIT 10;
```

---

## 🤝 المساهمة

Pull Requests مرحب بها! للتغييرات الكبيرة، افتح Issue أولاً.

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر - استخدمه كيفما تشاء!

---

## 🎯 خارطة الطريق

- [ ] دعم أقسام متعددة
- [ ] إشعارات Discord
- [ ] واجهة ويب للإحصائيات
- [ ] AI لتحليل الأسعار والتوقعات

---

## 👨‍💻 المطور

صُنع بـ ❤️ لمتابعة أفضل الصفقات

**تواصل معنا:** [رابط التواصل]

---

</div>

## 🌟 Star History

إذا أعجبك المشروع، لا تنسَ ⭐ النجمة!
